# Damage Claim AI Agent — Evaluation Report

> **Note:** This report is auto-generated by `evaluation/evaluate.py`. Run that script with real API credentials and sample images to populate actual metrics.

## System Architecture

The system uses **Claude claude-sonnet-4-6** (Vision) to analyze damage claims in a single multi-modal API call per claim. Each call bundles:
- All claim images (base64 encoded)
- User claim text
- User history context
- Evidence requirements
- Structured output instructions

## Processing Metrics

| Metric | Sample Set (5 claims) | Projected Test Set (50 claims) |
|--------|-----------------------|-------------------------------|
| Model calls | 5 | ~50 |
| Images per call (avg) | ~1.5 | ~1.5 |
| Total images processed | ~7–8 | ~75 |
| Input tokens (avg/call) | ~4,000–8,000 | — |
| Output tokens (avg/call) | ~200–400 | — |
| Elapsed time | ~15–30s | ~8–15 min |
| Avg latency per call | ~3–6s | ~3–6s |

## Cost Estimation

**Pricing (Claude claude-sonnet-4-6, 2025):**
- Input: $3.00 / 1M tokens
- Output: $15.00 / 1M tokens
- Image token cost: ~1,600 tokens per standard image (scales with resolution)

**Estimated cost for 50 test claims (avg 1.5 images/claim):**
- Input tokens: ~50 × 6,000 = 300,000 → **~$0.90**
- Output tokens: ~50 × 300 = 15,000 → **~$0.23**
- **Total estimated cost: ~$1.13 USD**

## TPM / RPM Considerations

- **claude-sonnet-4-6 rate limits**: ~4,000 RPM, 400K TPM (standard tier)
- **Strategy**: Sequential processing with 0.5s delay between calls
- At ~6,000 input tokens/call × 50 calls = 300K tokens → within TPM limits
- No batching API used (not yet available for vision tasks with complex prompts)
- **Retry strategy**: Not implemented in current version. For production: exponential backoff (1s → 2s → 4s → max 30s)

## Batching and Caching Strategy

| Strategy | Applied? | Notes |
|----------|----------|-------|
| Multi-image per claim | ✅ Yes | All images for a claim sent in one call |
| Cross-claim image caching | ❌ No | Each image is unique per claim |
| System prompt caching | 🔜 Recommended | Stable prompt structure could use prompt caching |
| Async concurrent calls | 🔜 Recommended | Use asyncio semaphore (5 concurrent) for speed |
| Image resizing | 🔜 Recommended | Resize to max 1024px to cut image tokens by ~40% |

## Accuracy Metrics (Sample Set)

> Run `python evaluation/evaluate.py` to populate these with real numbers.

| Field | Accuracy |
|-------|----------|
| claim_status | — |
| issue_type | — |
| object_part | — |
| severity | — |
| evidence_standard_met | — |
| valid_image | — |

## Operational Recommendations for Production

1. **Add image preprocessing**: Resize images to max 1024×1024 before encoding (reduces token cost ~40%)
2. **Implement prompt caching**: The system prompt is stable; use Anthropic's caching header to save ~80% of prompt token cost
3. **Add async batch processing**: Use `asyncio.Semaphore(5)` to process 5 claims in parallel, reducing wall time from 15 min → ~3 min
4. **Add retry logic**: Wrap API calls in exponential backoff for 529 (overload) errors
5. **Log all API calls**: Store `usage.input_tokens`, `usage.output_tokens`, and `response_time` per claim for auditing
6. **Add confidence threshold**: If model confidence is low, auto-route to `manual_review_required`
